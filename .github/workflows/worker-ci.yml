name: Worker CI

on:
  pull_request:
    paths:
      - 'apps/worker/**'
      - 'packages/db/**'
      - 'packages/shared/**'
      - 'packages/line-sdk/**'
      - 'packages/update-engine/**'
      - 'pnpm-lock.yaml'
      - '.github/workflows/worker-ci.yml'
      - 'scripts/check-redacted-logs.mjs'
  push:
    branches: [main]
    paths:
      - 'apps/worker/**'
      - 'packages/db/**'
      - 'packages/shared/**'
      - 'packages/line-sdk/**'
      - 'packages/update-engine/**'
      - 'pnpm-lock.yaml'
      - '.github/workflows/worker-ci.yml'
      - 'scripts/check-redacted-logs.mjs'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  worker-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      # Staged regression guard: fails if already-redacted files reintroduce raw
      # error logging. Partial scope by design (see scripts/check-redacted-logs.mjs).
      - run: pnpm lint:logs
      - run: pnpm --filter @line-crm/shared --filter @line-crm/line-sdk --filter @line-crm/db --filter @line-harness/update-engine build
      - run: pnpm --filter worker typecheck
      - run: pnpm --filter worker test
      - run: pnpm --filter worker build
        env:
          VITE_LIFF_ID: ${{ vars.VITE_LIFF_ID }}
          VITE_BOT_BASIC_ID: ${{ vars.VITE_BOT_BASIC_ID }}
          VITE_CALENDAR_CONNECTION_ID: ${{ vars.VITE_CALENDAR_CONNECTION_ID }}
